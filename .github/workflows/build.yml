name: Build APK shortcut

on:
  workflow_dispatch:

jobs:
  use-main-apk-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Use the real APK workflow
        run: echo "Lance le workflow 'Build & Upload APK' : il build l'app web, synchronise Capacitor Android, compile l'APK puis l'upload."
